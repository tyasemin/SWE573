services:
  db:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_DB: dotsdb
      POSTGRES_USER: dotsuser
      POSTGRES_PASSWORD: supersecurepass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network

  web:
    build: .
    command: >
      sh -c "python manage.py migrate &&
             python create_superuser.py &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./backend:/code
      - .env:/code/.env
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - .env
    working_dir: /code
    networks:
      - app-network

volumes:
  postgres_data:

networks:
  app-network:

